#############
# Generate Number of Peptides  ----

# Counting the number of unique peptides per run
#############


Generate_NumberOfPeptides <- function(dr, colors) {
  dr <- Generate_DiannReport(samples)
}